name: version_manager
description: A Dart package to automate version management for Flutter apps via the command line.
version: 0.0.2+1
homepage: https://github.com/royacodes/version_manager.git

environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  yaml: ^3.1.0
  args: ^2.3.0

dev_dependencies:
  test: ^1.20.0

executables:
  version_manager: version_manager.dart